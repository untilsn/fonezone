import express from 'express';

const userRouter = express.Router();

userRouter.get('/:id',);
userRouter.put('/update/:id',);
userRouter.put('/change-password/:id',);
userRouter.put('/forgot-password/:id',);



export default userRouter;